<ui:FluentWindow x:Class="Steamedpass.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Steamedpass.App"
        mc:Ignorable="d"
        Title="steamedpass" Height="640" Width="820"
        MinHeight="480" MinWidth="640"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Title="steamedpass" Grid.Row="0" />

        <Grid Grid.Row="1" Margin="20,8,20,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ui:TextBlock Grid.Row="0" Text="Installed Game Pass apps" FontTypography="BodyStrong" Margin="0,0,0,8" />

            <ui:Card Grid.Row="1" Padding="0">
                <ui:ListView x:Name="GamesList" BorderThickness="0" Background="Transparent">
                    <ui:ListView.View>
                        <ui:GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="260" />
                            <GridViewColumn Header="Executable" DisplayMemberBinding="{Binding Executable}" Width="200" />
                            <GridViewColumn Header="AUMID" DisplayMemberBinding="{Binding Aumid}" Width="280" />
                        </ui:GridView>
                    </ui:ListView.View>
                </ui:ListView>
            </ui:Card>

            <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,14,0,0">
                <ui:Button x:Name="RefreshButton" Content="Refresh" Icon="{ui:SymbolIcon ArrowClockwise24}" Margin="0,0,8,0" Click="RefreshButton_Click" />
                <ui:Button x:Name="AddButton" Content="Add to Steam" Icon="{ui:SymbolIcon Add24}" Appearance="Primary" Margin="0,0,8,0" Click="AddButton_Click" />
                <ui:Button x:Name="SettingsButton" Content="Settings" Icon="{ui:SymbolIcon Settings24}" Click="SettingsButton_Click" />
            </StackPanel>

            <TextBlock x:Name="StatusText" Grid.Row="3" Margin="0,10,0,0" TextWrapping="Wrap" Opacity="0.8" />
        </Grid>
    </Grid>
</ui:FluentWindow>
